# Base URL of the AddressBook API under test (must end with /api/).
BASE_URL=http://localhost:5000/api/

# Per-request timeout in milliseconds.
API_TIMEOUT=30000

# Default expect() assertion timeout in milliseconds.
EXPECT_TIMEOUT=5000
